/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "cdn.iconscout.com",
            "www.edigitalagency.com.au"
        ]
    }
};

export default nextConfig;
